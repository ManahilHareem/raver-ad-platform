# API Configuration
# The base URL for the backend API endpoints (e.g., http://localhost:8000/api)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# The base URL for the backend server (e.g., http://localhost:8000)
# Used for static assets or websocket connections if applicable
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Feature Flags
# NEXT_PUBLIC_ENABLE_ANALYTICS=false
